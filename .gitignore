# Go
api/bin/

# Azure Functions
api/local.settings.json
api/.python_packages/
**/.azurefunctions/
*.zip

# Azurite
.azurite/
__azurite_db*.json
__blobstorage__/
__queuestorage__/

# Node / SvelteKit (web/.gitignore covers the rest)
node_modules/

# Python (sources/tools)
.venv/
__pycache__/
*.py[cod]

# Source extractor output (blogs.yml is committed, these are not)
sources/blogs.sample.yml
sources/tools/link-audit.csv
sources/tools/build.log

# Editors and OS (web/.vscode is shared and stays committed)
/.vscode/
.DS_Store
Thumbs.db

# Written by infra/repair-authors.sh before it patches the index.
author-repair-rollback.json

# Written by infra/repair-summaries.sh: the summaries it records before patching the
# index, and the slices it has finished so an interrupted run can resume.
summary-repair-rollback.jsonl.gz
summary-repair-checkpoint.json
